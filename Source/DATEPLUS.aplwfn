R←YMD DATEPLUS N;G;I;M;S;T;U;V;W;Y;∆YMD

⍝ Returns date that is N calendar days after date YYYYMMDD. TEST

⍝ Returns 0 if date (given or resulting) is invalid.

⍝ Arguments are scalar conformable.  Needs:  ci

 M←×/S←⍴YMD+N ⋄ N←M⍴N ⍝ Keep shape of arguments to reshape result

 ∆YMD← 0 100 100 ⊤YMD←M⍴YMD ⍝ YYYYMMDD → YYYY MM DD

⍝ Index of YMD into raveled (2⊃ci):

 I←∆YMD[3;]+31×(∆YMD[2;]-1)+12×¯1+(Y←1⊃ci)⍳∆YMD[1;]

 W←WHERE,(2⊃ci)≠'X' ⍝ Indices of calendar days into raveled (2⊃ci)

 T←W⍳I ⋄ U←N+T ⋄ R←⍬⍴⍴W ⋄ V←1⌈R⌊U

⍝ Do those that fall within the calendar:

 G←(T≤R)^V=U ⋄ I←1+ 0 12 31 ⊤¯1+W[G/V]

 R←I[3;]+100×I[2;]+100×Y[I[1;]] ⍝ Indices → YYYYMMDD

⍝ Now those outside the calendar, using TODAYS/FROMDAYS:

 →(^/G)⍴L1 ⋄ R←G\R ⋄ G←~G

 (G/R)←FROMDAYS (G/N)+TODAYS G/YMD

L1:R←S⍴R

